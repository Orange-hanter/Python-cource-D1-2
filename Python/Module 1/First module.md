Python:

* procs and cons of Python

* интерпретатор Python/виды/альтернативные реализации обзорно в 2ух словах  

* python program execution flow, PythonVirtualMachine+bytecode, module dis,   

* what is object, object main attributes. кеширование/оптимизации (strings interning, little ints caching)  

* виды типизации, типизация в Python и почему  

* сравнение в Python, == vs 'is' operator

  

Linux:

* Linux: files/dirs how to create, delete, move etc basic ops in shell console  
* what is file data/metadata, how to show metadata (size, owner etc.)  
* file permissions/ownerships  
* hard link vs soft link, differences and howto